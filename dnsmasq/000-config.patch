diff -BurpN dnsmasq-2.60/src/config.h dnsmasq/src/config.h
--- dnsmasq-2.60/src/config.h	2012-03-04 23:04:22.000000000 +0300
+++ dnsmasq/src/config.h	2012-03-06 13:24:13.000000000 +0300
@@ -116,11 +116,11 @@ RESOLVFILE
    has no library dependencies other than libc */
 
 #define HAVE_DHCP
-#define HAVE_DHCP6 
-#define HAVE_TFTP
-#define HAVE_SCRIPT
+/* #define HAVE_DHCP6 */
+/* #define HAVE_TFTP */
+/* #define HAVE_SCRIPT */
 /* #define HAVE_LUASCRIPT */
-/* #define HAVE_BROKEN_RTC */
+#define HAVE_BROKEN_RTC
 /* #define HAVE_DBUS */
 /* #define HAVE_IDN */
 /* #define HAVE_CONNTRACK */
@@ -200,10 +200,13 @@ HAVE_SOCKADDR_SA_LEN
 #if !defined(__ARCH_HAS_MMU__) && !defined(__UCLIBC_HAS_MMU__)
 #  define NO_FORK
 #endif
-#if defined(__UCLIBC_HAS_IPV6__)
+#if defined(__UCLIBC_HAS_IPV6__) && defined(USE_IPV6)
 #  ifndef IPV6_V6ONLY
 #    define IPV6_V6ONLY 26
 #  endif
+#else
+#  undef HAVE_IPV6
+#  define NO_IPV6
 #endif
 
 /* This is for glibc 2.x */
