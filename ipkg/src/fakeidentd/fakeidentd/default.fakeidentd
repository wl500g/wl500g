# This is a configuration file for /opt/etc/init.d/fakeidentd;
# it allows you to perform common modifications to the behavior of
# the fakeidentd daemon startup without editing the init script.

# Parameters to pass to fakeidentd.
PARAMS=""

# Whether or not to run the identd system daemon; set to 0 to disable.
ENABLED=1
