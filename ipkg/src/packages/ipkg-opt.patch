--- ipkg-0.99.144/args.h	2005-02-23 02:00:52.000000000 +0300
+++ ipkg/args.h	2005-03-12 15:26:16.000000000 +0300
@@ -43,7 +43,7 @@
 };
 typedef struct args args_t;
 
-#define ARGS_DEFAULT_CONF_FILE_DIR "/etc"
+#define ARGS_DEFAULT_CONF_FILE_DIR "/opt/etc"
 #define ARGS_DEFAULT_CONF_FILE_NAME "ipkg.conf"
 #define ARGS_DEFAULT_DEST NULL
 #define ARGS_DEFAULT_FORCE_DEFAULTS 0
--- ipkg-0.99.144/ipkg_conf.c	2005-02-23 01:45:14.000000000 +0300
+++ ipkg/ipkg_conf.c	2005-03-12 15:11:40.000000000 +0300
@@ -103,7 +103,7 @@
      nv_pair_list_t tmp_dest_nv_pair_list;
      char * lists_dir =NULL;
      glob_t globbuf;
-     char *etc_ipkg_conf_pattern = "/etc/ipkg/*.conf";
+     char *etc_ipkg_conf_pattern = "/opt/etc/ipkg/*.conf";
      char *pending_dir  =NULL;
 
      memset(conf, 0, sizeof(ipkg_conf_t));
@@ -185,7 +185,7 @@
      conf->pending_dir = strdup(pending_dir);
 
      if (args->offline_root) 
-	  sprintf_alloc(&etc_ipkg_conf_pattern, "%s/etc/ipkg/*.conf", args->offline_root);
+	  sprintf_alloc(&etc_ipkg_conf_pattern, "%s/opt/etc/ipkg/*.conf", args->offline_root);
      memset(&globbuf, 0, sizeof(globbuf));
      err = glob(etc_ipkg_conf_pattern, 0, NULL, &globbuf);
      if (!err) {
--- ipkg/update-alternatives~	2005-04-10 13:49:47.000000000 -0700
+++ ipkg/update-alternatives	2005-05-29 21:50:30.000000000 -0700
@@ -21,7 +21,7 @@
 set -e
 
 # admin dir
-ad="$IPKG_OFFLINE_ROOT/usr/lib/ipkg/alternatives"
+ad="$IPKG_OFFLINE_ROOT/opt/lib/ipkg/alternatives"
 
 usage() {
 	echo "update-alternatives: $*
