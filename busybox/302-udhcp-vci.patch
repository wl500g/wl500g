diff -BurpN busybox-1.14.2/networking/udhcp/clientpacket.c busybox/networking/udhcp/clientpacket.c
--- busybox-1.14.2/networking/udhcp/clientpacket.c	2009-07-22 17:51:32.000000000 +0000
+++ busybox/networking/udhcp/clientpacket.c	2009-07-22 17:54:36.000000000 +0000
@@ -48,7 +48,7 @@ static void init_packet(struct dhcpMessa
 		add_option_string(packet->options, client_config.hostname);
 	if (client_config.fqdn)
 		add_option_string(packet->options, client_config.fqdn);
-	if ((type != DHCPDECLINE) && (type != DHCPRELEASE))
+	if ((type != DHCPDECLINE) && (type != DHCPRELEASE) && (client_config.vendorclass))
 		add_option_string(packet->options, client_config.vendorclass);
 }
 
