--- a/hotplug2.c	2009-10-19 20:57:44.000000000 +0400
+++ b/hotplug2.c	2009-10-19 20:58:04.000000000 +0400
@@ -526,6 +526,8 @@ end_rules:
 	while (!terminate) {
 		size = recv(netlink_socket, &buffer, sizeof(buffer), 0);
 		recv_errno = errno;
+		if (size < 0)
+			continue;
 	
 		tmpevent = get_hotplug2_event(buffer, size);
 		
